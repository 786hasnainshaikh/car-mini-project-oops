public class Main {
    public static void main(String[] args) {
        carola obj=new carola();
        obj.getname("carola");
        obj.move(30,45);
        obj.steer(45);
        obj.change_gear(4);
        obj.iscarmanual(1);
        obj.max_speed();
    }
}